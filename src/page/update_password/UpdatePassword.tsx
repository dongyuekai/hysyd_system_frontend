import { useNavigate } from "react-router-dom";
import { Form, Input, Button, message } from "antd";
import { } from "../../interface/interfaces.ts";
import React from "react";

// ... 其余代码保持不变 

export function UpdatePassword() {
  return <div>UpdatePassword</div>
}